# ============================================
# Bifrost Polkadot Network Configuration
# (Used in main branch)
# ============================================
VITE_BIFROST_RPC_URL=wss://bifrost-rpc.liebi.com/ws

# Application Configuration
VITE_APP_NAME="Bifrost Flow"
# Override the default Bifrost RPC endpoint (optional).
# Leave unset to use the baked-in Liebi/Radium fallback list.
# Example: pin the HK node for low latency during demos.
# VITE_BIFROST_RPC=wss://hk.p.bifrost-rpc.liebi.com/ws

# ============================================
# EVM Network Configuration
# (Used in evm branch)
# ============================================

# WalletConnect Project ID (required for RainbowKit)
# Get one at https://cloud.walletconnect.com/
VITE_WALLETCONNECT_PROJECT_ID=

# Optional: Custom RPC URLs (leave empty to use public RPCs)
# VITE_RPC_URL_ARBITRUM_SEPOLIA=
# VITE_RPC_URL_BASE_SEPOLIA=
# VITE_RPC_URL_SEPOLIA=
